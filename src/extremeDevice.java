public class extremeDevice extends Device {
    public extremeDevice(int minWeight, int minHeight, int minAge, boolean isOpen, String name, float price) {
        super(minWeight, minHeight, minAge, isOpen, name, price);
    }
}